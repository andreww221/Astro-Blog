---

interface Props{
    smallMarginTop?:boolean
}

const {smallMarginTop} = Astro.props;

---

<hr class:list={[
    `w-12/12 h-0.5 border-0 bg-neutral-200 mx-auto mt-8   dark:bg-neutral-700`,
    {
        "mt-5!":smallMarginTop,
    }
]} />